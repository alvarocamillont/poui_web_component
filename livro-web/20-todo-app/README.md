# Todo App with lit-html

To build this app run `npm run build` in the root of the example
directory with the `package.json`. To run all code examples run
`npm start` in the root of the `code` directory.
